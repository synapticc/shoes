<form title="Delete review" method="post" action="{{ path('admin_review_delete', {'review': review.id}) }}" onsubmit="return confirm('Are you sure you want to delete this review?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ review.id) }}">
    <button class="btn btn-danger">Delete review</button>
</form>
