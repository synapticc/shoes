{% extends 'base.html.twig' %}

{% block title %}{{app.current_route =='admin_product_data_new' ?  'New' : 'Edit'}} {{product.name}} {{product.type_full}} | Admin{% endblock %}

{% block stylesheets %}{{ encore_entry_link_tags('admin') }}{% endblock %}

{% block body %}
    <div class="bg-soft">
      <!-- top navigation -->
      {% include 'admin/_partials/navbar.html.twig' %}
        <div class="container-fluid bg-{{ app.user.theme == 'light' ? 'soft' : 'dark' }}">
           <div class="row">
              <div class="col-12">
                <!-- Left sidebar  -->
                {% include 'admin/_partials/left-sidebar.html.twig' %}
                <!-- edit details form  -->
                {% include 'admin/2_product_data/partials/_form.html.twig' %}
              </div>
           </div>
        </div>
    </div>

    {% block javascripts %}{{ encore_entry_script_tags('admin') }}{% endblock %}
  {% endblock %}
